apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home-assistant-dbus-pv-claim
  labels:
    io.kompose.service: home-assistant-dbus-pv-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
